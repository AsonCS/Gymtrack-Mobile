name: Test ADM CI

on:
  push:
    branches: [ "test-adm" ]

#{"gradleTask":"app:packageDevApi28DebugUniversalApk","pathApk":"./app/build/outputs/apk_from_bundle/devApi28Debug/app-dev-api28-debug-universal.apk","pathFilesBoxcontrol":"test-ci/devApi28Debug"}
jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: echo
        env:
          commitMessage: ${{ fromJSON(github.event.head_commit.message) }}
        run: |
#          echo $commitMessage
           echo "${{ $commitMessage.gradleTask }}"
#          echo "${{ COMMIT_PARAMS.gradleTask }}"
#          echo "${{ COMMIT_PARAMS.pathApk }}"
#          echo "${{ COMMIT_PARAMS.pathFilesBoxcontrol }}"
#          echo "${{ COMMIT_PARAMS.test }}"

#      - name: stopJob
#        if: ${{ env.commitParams.gradleTask == '' || env.commitParams.pathApk == '' || env.commitParams.pathFilesBoxcontrol == '' || env.commitParams.test == '' }}
#        run: |
#          exit 1

#      - uses: actions/checkout@v4
#      - name: set up JDK 17
#        uses: actions/setup-java@v4.6.0
#        with:
#          java-version: '17'
#          distribution: 'jetbrains'
#          cache: gradle
#
#      - name: Grant execute permission for gradlew
#        run:
#          chmod +x gradlew
#
#      - name: Create google-services.json file
#        env:
#          GOOGLE_SERVICES_JSON: ${{ secrets.ADM_GOOGLE_SERVICES_JSON }}
#        run:
#          echo $GOOGLE_SERVICES_JSON > ./composeApp/src/google-services.json
#
#      - name: Generate BuildConfigGenerator
#        run:
#          ./gradlew buildConfigGenerator
#
#      - name: Build with Gradle
#        run:
#          ./gradlew :composeApp:packageDebug
#
#      - name: Publish on files.boxcontrol.io
#        env:
#          APK_NAME: composeApp-debug
#          APK_PATH: ./composeApp/build/outputs/apk/debug/composeApp-debug.apk
#          USER_NAME: ${{ secrets.ADM_FILES_BOXCONTROL_USER_NAME }}
#          USER_NAME_URL: ${{ secrets.ADM_FILES_BOXCONTROL_USER_NAME_URL }}
#          USER_PASSWORD: ${{ secrets.ADM_FILES_BOXCONTROL_USER_PASSWORD }}
#        run:
#          curl -u "${USER_NAME}":"${USER_PASSWORD}" -T "${APK_PATH}" "https://files.boxcontrol.io/remote.php/dav/files/${USER_NAME_URL}/adm-team/test-ci/${APK_NAME}.apk"
